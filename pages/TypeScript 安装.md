tags:: [[TypeScript]]
---

- ## 安装方式
	- 有如下安装方式:
		- 安装为 npm module
		  logseq.order-list-type:: number
		- 安装为 NuGet package
		  logseq.order-list-type:: number
		- 安装为 Visual Studio Extension
		  logseq.order-list-type:: number
- ## 安装为 npm module
	- ### 全局安装
		- ``` zsh
		  npm install -g typescript
		  pnpm install -g typescript
		  yarn install -g typescript
		  ```
	- ### 项目级安装 (首选)
		- 应首选在项目内安装 (且为 **开发依赖** ), 以便代码库可以在不同机器上产生相同的构建产物.
		- ``` zsh
		  npm install typescript --save-dev
		  pnpm install typescript --save-dev
		  yarn install typescript --save-dev
		  ```
- ## 参考
	- [TypeScript Download](https://www.typescriptlang.org/download/)
	  logseq.order-list-type:: number