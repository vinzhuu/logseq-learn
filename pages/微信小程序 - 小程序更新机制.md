tags:: [[微信小程序]]
---

- ## 小程序代码包
	- 首先, 必须知道的一点是: **微信客户端** 启动 **小程序** 前, 必须先将 **小程序代码包** 下载到本地.
	- 也即, 打开 **小程序** 时, **微信客户端** 访问的是 **本地离线保存** 的 **小程序代码包** , 而非 **实时访问** 服务器上的 **小程序代码包** .
- ## 小程序更新步骤
	- 小程序更新可以分为如下几步:
		- **检查** : **微信客户端** 在恰当的时机, **检查** 本地已下载的小程序, 是否有新版本.
		  logseq.order-list-type:: number
		- **下载** : **微信客户端** 在恰当的时机, **下载** 新版本代码包.
		  logseq.order-list-type:: number
		- **使用** : **微信客户端** 在某次启动小程序时, **使用** 新版本代码包启动.
		  logseq.order-list-type:: number
- ## 具体更新流程
	- ### 涉及的流程
		- 小程序更新, 主要涉及两个流程, 二者独立, 互不影响 (或许会有协调机制, 保证二者在检查更新时不会冲突):
			- **微信客户端** 后台定期版本 **检查** .
			  logseq.order-list-type:: number
			- **微信客户端** 启动小程序.
			  logseq.order-list-type:: number
	- ### 后台定期版本检查 流程
		- 定期 **检查** 本地已下载的小程序, 是否有新版本.
			- 如果有, 则将 **小程序** 标记为 **待更新** .
	- ### 启动小程序 流程
		- 用户启动小程序. (不管是 **冷启动** 还是 **热启动** )
		  logseq.order-list-type:: number
		- 判断: 本地是否已有小程序缓存?
		  logseq.order-list-type:: number
			- 否: 则同步 **下载** 最新版本的代码包.
			  logseq.order-list-type:: number
				- ==成功: 则 **使用** 最新版本的代码包启动==
				  logseq.order-list-type:: number
				- ==失败: 则无法启动== (比如 下载失败或超时)
				  logseq.order-list-type:: number
			- 是, 则下一步.
			  logseq.order-list-type:: number
		- 判断: 小程序是否 **待更新** ?
		  logseq.order-list-type:: number
			- 是, 则同步 **下载** 最新版本的代码包. (此时, 会阻塞小程序的启动流程, 影响小程序的 **启动耗时** )
			  logseq.order-list-type:: number
				- ==成功: 则 **使用** 最新版本的代码包启动==
				  logseq.order-list-type:: number
				- ==失败: 则 **使用** 旧版本代码包启动== (比如 下载失败或超时)
				  logseq.order-list-type:: number
			- 否, 则下一步.
			  logseq.order-list-type:: number
		- 判断: 小程序是否 **长时间未使用** ?
		  logseq.order-list-type:: number
			- 是, 则 **检查** 是否有新版本 ?
			  logseq.order-list-type:: number
				- 是, 则同步 **下载** 最新版本的代码包. (此时, 会阻塞小程序的启动流程, 影响小程序的 **启动耗时** )
				  logseq.order-list-type:: number
					- ==成功: 则 **使用** 最新版本的代码包启动==
					  logseq.order-list-type:: number
					- ==失败: 则 **使用** 旧版本代码包启动== (比如 下载失败或超时)
					  logseq.order-list-type:: number
				- ==否, 则 **使用** 旧版本代码包启动==
				  logseq.order-list-type:: number
			- 否, 则下一步.
			  logseq.order-list-type:: number
		- 判断: 属于哪种启动流程?
		  logseq.order-list-type:: number
			- 热启动: 
			  logseq.order-list-type:: number
			- ==冷启动: 则启动旧版本 **使用** , 同时开启异步版本 **检查** (具体见下文) ==
			  logseq.order-list-type:: number
	- ### 冷启动异步版本检查 流程
		- ==注意: 当前流程是异步的, 不回阻塞小程序启动. ==
		- **检查** 是否有新版本 ?
		  logseq.order-list-type:: number
			- 是, 则 **下载** 最新版本的代码包.
			  logseq.order-list-type:: number
			- ==否, 则不处理==
			  logseq.order-list-type:: number
		- 下一次 **冷启动** 时, **使用** 最新版本的代码包.
		  logseq.order-list-type:: number
- ## UpdateManager
	- https://developers.weixin.qq.com/miniprogram/dev/api/base/update/UpdateManager.html#%E7%A4%BA%E4%BE%8B%E4%BB%A3%E7%A0%81
- ## 问题
	- 热启动到底会不会执行版本检查?
	  logseq.order-list-type:: number
	- 新版本的小程序需要等**下一次冷启动**才会使用? 下一次热启动不会使用吗?
	  logseq.order-list-type:: number
		- 另外, 下一次启动时, 是如何判断要使用已经下载的新版本呢? 我上面编写的逻辑是不是要变动下?
- ## 参考
	- [小程序运行时 - 更新机制](https://developers.weixin.qq.com/miniprogram/dev/framework/runtime/update-mechanism.html)
	  logseq.order-list-type:: number
	- ChatGPT
	  logseq.order-list-type:: number